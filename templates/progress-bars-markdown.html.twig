{% set maxLabelLength = 26 %}
{% set maxDescriptionLength = 17 %}
**{{ title }}**

```text
{% for progressBar in progressBars %}
{{ progressBar.getLabel() }}{{ ' '|repeat(maxLabelLength - (progressBar.getLabel() | length)) }}{{ progressBar.getQuantityDescription() }}{{ ' '|repeat(maxDescriptionLength - (progressBar.getQuantityDescription() | length)) }}{{ '█'|repeat((progressBar.getPercentage() / 4)|round) }}{{ '░'|repeat(25 - (progressBar.getPercentage() / 4)|round) }}   {{ progressBar.getPercentage()|number_format(2, '.', '') }} %
{% endfor %}
```